# Normaliza finais de linha no repositorio.
* text=auto

# Scripts executados dentro de containers Linux precisam de LF.
# Com CRLF, o shell do Alpine falha em `set -e` ("illegal option -").
*.sh text eol=lf
Dockerfile text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.conf text eol=lf
